#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;

int main(){
    int t, n;
    cin >> t;

    while (t-- > 0) {
        cin >> n;
        cout << 2 * (n - 1) << endl;
    }
    
    return 0;
}