#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;
using vii = vector<vi>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n;
    cin >> n;

    for (int i = 0; i < x; i++) {
        for(int j = 0; j < y; j++) {

        }
    }

    for (int i = 0; i < x; i++) {

    }

    cout << s << '\n';

    return 0;
}