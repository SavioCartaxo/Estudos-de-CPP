#include <bits/stdc++.h>
using namespace std;

int main(){
    stack<int> pilha;
    // Adiciona
    pilha.push(20);
    
    // Tamanho
    pilha.size();
    
    // Primeiro
    pilha.top();

    // Removes
    pilha.pop();

    return 0;
}